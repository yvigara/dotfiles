status is-interactive; or exit

# ── editor ────────────────────────────────────────────────────────────────────
alias vi=nvim
alias vim=nvim
alias vimdiff='nvim -d'

# ── listing (eza wrapper is in functions.d/aliases/ls.fish) ───────────────────
alias l='ls -l'
alias ll='ls -lah'
alias la='ls -lah --sort modified'
alias lt='ls -T'
alias llt='ls -lahT'

# ── git ───────────────────────────────────────────────────────────────────────
abbr -a -- gitds 'git ds'
abbr -a -- gitadd 'git add'
abbr -a -- g git
abbr -a -- gs 'git status'
abbr -a -- ga 'git add'
abbr -a -- gc 'git commit'
abbr -a -- gcm 'git commit -m'
abbr -a -- gca 'git commit --amend'
abbr -a -- gp 'git push'
abbr -a -- gpl 'git pull'
abbr -a -- gd 'git diff'
abbr -a -- gst 'git diff --staged'
abbr -a -- gco 'git checkout'
abbr -a -- gb 'git branch'
abbr -a -- gl 'git log --oneline --graph --decorate'

# ── gh ────────────────────────────────────────────────────────────────────────
abbr -a -- ghw 'gh repo view --web'
abbr -a -- gpc 'gh pr create -a "@me" --fill'
abbr -a -- ghpr 'gh pr create -a "@me" --fill'
abbr -a -- ghd 'gh dash'

# ── chezmoi ───────────────────────────────────────────────────────────────────
abbr -a -- cz chezmoi
abbr -a -- cza 'chezmoi add'
abbr -a -- czap 'chezmoi apply --init'
abbr -a -- cze 'chezmoi edit'
abbr -a -- czd 'chezmoi diff'
abbr -a -- czu 'chezmoi update --init'

# ── package managers ─────────────────────────────────────────────────────────
alias pn=pnpm
alias px='pnpm dlx'
alias bru='brew update && brew upgrade --yes'

# ── docker ────────────────────────────────────────────────────────────────────
alias dk=docker
alias dc='docker compose'
abbr -a -- dsp 'docker system prune'

# ── kubernetes ────────────────────────────────────────────────────────────────
alias k=kubectl
abbr -a -- kgp 'kubectl get pods'
abbr -a -- kgs 'kubectl get svc'
abbr -a -- kgn 'kubectl get nodes'

# ── terraform ─────────────────────────────────────────────────────────────────
alias tf=terraform
abbr -a -- tfi 'tf init'
abbr -a -- tfp 'tf plan'
abbr -a -- tfa 'tf apply'

# ── system ────────────────────────────────────────────────────────────────────
abbr -a -- reload 'exec fish'
abbr -a -- ip 'dig +short myip.opendns.com @resolver1.opendns.com'
abbr -a -- ipl 'ipconfig getifaddr en0'
abbr -a -- flushdns 'sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder'

{{- if eq .chezmoi.os "darwin" }}
abbr -a -- oa 'open -a'
abbr -a -- oo 'open .'
{{- end }}

# ── Claude Code ───────────────────────────────────────────────────────────────
alias cc='claude'
